# 生产环境配置（Electron打包后使用）
# 备注（2026-02-25）：打包后的exe连接服务器后端

# API基础地址（服务器地址）
VITE_API_BASE_URL=http://8.138.59.152:8001/api

# WebSocket地址（如果需要）
VITE_WS_BASE_URL=ws://8.138.59.152:8001/ws

# 备注：
# 1. 打包时会自动使用这个文件中的配置
# 2. 修改为实际服务器地址后需要重新打包
# 3. 如果使用域名，请确保CORS配置正确
